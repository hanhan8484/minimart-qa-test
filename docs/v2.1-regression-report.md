# v2.1 迴歸測試報告

> 面試交付第 5 項

## 基本資料

| 項目 | 內容 |
|---|---|
| SUT 版本 | **v2.1**（面試官：伺服器端已換成 `minimart:v2.1`，**BASE_URL 不變**） |
| `BASE_URL` | `https://cand1.tail296b14.ts.net` |
| 測試日期 | 2026-07-20 |
| 對照 | `PRD.md`（v2.1）+ 下方 RD Changelog 對照 |
| 全量執行 | `npm test`（chromium，`workers: 1`） |
| HTML 報告 | [`docs/reports/latest/`](./reports/latest/) |
| 原文增補稿 | `docs/PRD-v2.1-supplement.docx`（已併入 `PRD.md`，僅作來源留存） |

## 環境認定

面試官說明：專屬環境已更新為 v2.1，**不必換網址**。  
因此本報告所有失敗／通過皆視為 **v2.1 行為**，不是連錯環境。

## RD Changelog 對照

來源：RD「MiniMart v2.1 更新說明」。

### 已修復

| Changelog | 本庫 DEF | 自動化 | 複驗 |
|---|---|---|---|
| 登出後購物車未清空 | DEF-002 | `g-logout-clears-cart.spec.ts` | **Verified**（已移除 `test.fail`） |
| 訂單列表商品件數錯誤 | DEF-020 | `o-orders.spec.ts` O-B01 | **Verified** |
| 退款時間未記錄 | DEF-008 | `o-return-refund.spec.ts` | **未通過**（`refundedAt` 仍 null；已加回 `test.fail`） |

### 新增

| 功能 | 規則 | 自動化 | 複驗 |
|---|---|---|---|
| 訂單備註 | R-12.12、R-14.11、R-18.10（見 `PRD.md`） | C-B12／C-B13／O-B07 | **通過** |

### 環境注意

- Day-0 種子訂單 ID 隨伺服器 D0 日期滾動；`resetEnv` 以「3 筆 + 待出貨→已完成→已出貨」驗證，不再寫死 `MM-YYYYMMDD-…`。

## 全量結果摘要（2026-07-20）

| 項目 | 數值 |
|---|---|
| Playwright 列印 | **98 passed／1 failed**（約 18.5 分鐘） |
| 意外失敗 | **1**：`N-B01` 種子通知日期寫死（**測試債**；已改讀訂單 `createdAt`） |
| 預期失敗 | 其餘 `test.fail(true, 'DEF-xxx')`（已知未修缺陷） |

## v2.1 新發現／狀態更新

| ID | 標題 | 狀態 | 嚴重度 |
|---|---|---|---|
| **DEF-025** | 訂單列表改為 **舊→新**（違反 R-14.1 新→舊） | Confirmed（`test.fail`） | S3 |
| **DEF-026** | 訂單備註功能未交付 | **Verified（誤判關閉）** — 功能已交付 | — |
| **DEF-013** | 庫存不足仍可下單 | **Verified** — stock=0 馬克杯 checkout 正確回 409 | S2 |
| **DEF-014** | 非法狀態轉換 API 回 200 | **Verified（誤報關閉）** — PRD 未規定 4xx，操作前後狀態不變 | S2 |
| **DEF-027** | 購物車 API 接受負數 quantity | Confirmed（`test.fail`） | S2 |
| **DEF-028** | 購物車 API 允許加入 stock=0 商品 | Confirmed（`test.fail`）；checkout 最後防線正常 | S3 |
| **DEF-029** | 缺貨 checkout 回 409 後仍消耗優惠券 | Confirmed（`test.fail`） | S2 |

## 自動化更新摘要

- 種子 ID 改 D0 滾動 + 正規化新→舊
- 移除 DEF-002／020 的 `test.fail`；DEF-008 複驗失敗後加回
- 新增 C-B12／C-B13／O-B07（併入既有結帳／訂單 UI 檔）；`o-orders` DEF-025 `test.fail`
- `n-notifications` 種子下單時間改讀 API `createdAt`
- C-A06 改用初始 stock=0 馬克杯，獨立驗證 409 與失敗原子性；DEF-013 → Verified，另發現 DEF-029
- C-A02 新增負數 quantity（DEF-027）與售罄商品加車（DEF-028）負向案例
- O-A01～O-A05 重構為獨立 setup／response status／狀態前後比對；DEF-014 依 PRD invariant 複驗為誤報

## 建議回覆 RD／收卷結論

1. 登出清車、列表件數 **已驗證通過**（DEF-002／020）。  
2. 訂單備註 **已交付**（DEF-026 Verified）。  
3. 退款時間（DEF-008）**未修好**。  
4. 列表排序與 R-14.1 相反 → **DEF-025**。  
5. checkout 缺貨阻擋正常；但購物車 API 仍接受負數與售罄商品（DEF-027／028），且失敗 checkout 會消耗券（DEF-029）。
6. 全量報告已歸檔：`docs/reports/latest/`。

## 變更紀錄

| 日期 | 說明 |
|---|---|
| 2026-07-20 | 初稿；誤判「環境未更新」 |
| 2026-07-20 | 依面試官說明更正；002／020 Verified；025／026 登錄 |
| 2026-07-20 | DEF-008 複驗仍 Fail；加回 test.fail |
| 2026-07-20 | c-order-note 通過；DEF-026 Verified |
| 2026-07-20 | 訂單備註案例拆併：C-B12／C-B13／O-B07；刪除獨立 `c-order-note.spec.ts` |
| 2026-07-20 | 全量結果寫入；HTML 歸檔；修正 N-B01 D0 日期斷言 |
| 2026-07-20 | 併入原 `v2.1-changelog.md`；刪除探測過程檔 |
