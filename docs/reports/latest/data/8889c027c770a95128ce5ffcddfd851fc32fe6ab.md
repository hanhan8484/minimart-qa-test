# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: flows\o-cancel-order.spec.ts >> O-C02 cancel order within 10 minutes >> new order shows cancel dialog → 已取消
- Location: tests\flows\o-cancel-order.spec.ts:16:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('button', { name: '取消訂單' })
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByRole('button', { name: '取消訂單' })

```

```yaml
- banner:
  - link "MiniMart":
    - /url: /
  - navigation:
    - link "商品":
      - /url: /
    - link "購物車":
      - /url: /cart
    - link "我的訂單":
      - /url: /orders
    - link "我的優惠卷":
      - /url: /coupons
    - link "通知中心 4":
      - /url: /notifications
    - text: demo@minimart.test
    - button "登出"
- main:
  - heading "訂單詳情" [level=1]
  - heading "訂單資訊" [level=2]
  - text: 訂單編號 MM-20260721-0001 下單時間 2026-07-21 21:07 訂單狀態 待出貨 預計出貨日 2026-07-23
  - heading "商品明細" [level=2]
  - text: 手沖咖啡濾杯 NT$480 x1 NT$480
  - heading "金額摘要" [level=2]
  - text: 商品小計 NT$480 滿額折扣 NT$0 優惠券折抵 NT$0 運費 NT$80 應付金額 NT$560
  - heading "收件資訊" [level=2]
  - text: 收件人姓名 取消流程測試 手機號碼 0912345678 收件地址 台北市信義區測試路 1 號
  - heading "訂單備註" [level=2]
  - paragraph: （無備註）
  - heading "狀態與操作" [level=2]
  - button "模擬出貨（Demo）"
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import { loginAsDemo, placeOrderAndOpenDetail, resetEnv } from '../helpers';
  3  | 
  4  | /**
  5  |  * Batch 3 — O-C02 R-6.5 cancel within 10 minutes
  6  |  *
  7  |  * DEF-003: new 待出貨 order detail has no「取消訂單」button;
  8  |  * order JSON has no canCancel; POST …/cancel → 404.
  9  |  */
  10 | test.describe('O-C02 cancel order within 10 minutes', () => {
  11 |   test.beforeAll(async ({ request }) => {
  12 |     test.setTimeout(60_000);
  13 |     await resetEnv(request);
  14 |   });
  15 | 
  16 |   test('new order shows cancel dialog → 已取消', async ({ page }) => {
  17 |     test.setTimeout(90_000);
  18 |     test.fail(
  19 |       true,
  20 |       'DEF-003: 待出貨且未滿 10 分鐘仍無「取消訂單」按鈕／無 cancel API（R-6.5）',
  21 |     );
  22 | 
  23 |     await loginAsDemo(page);
  24 |     const orderId = await placeOrderAndOpenDetail(page, {
  25 |       name: '取消流程測試',
  26 |       phone: '0912345678',
  27 |       address: '台北市信義區測試路 1 號',
  28 |     });
  29 | 
  30 |     await expect(page.getByText('待出貨').first()).toBeVisible();
  31 |     const cancelBtn = page.getByRole('button', { name: '取消訂單' });
> 32 |     await expect(cancelBtn).toBeVisible();
     |                             ^ Error: expect(locator).toBeVisible() failed
  33 | 
  34 |     await cancelBtn.click();
  35 |     await expect(page.getByText('確定要取消這筆訂單嗎？')).toBeVisible();
  36 |     await page.getByRole('button', { name: '確定' }).click();
  37 | 
  38 |     await expect
  39 |       .poll(async () => {
  40 |         const main = await page.locator('main').innerText();
  41 |         const m = main.match(/訂單狀態\s*\n?\s*(待出貨|已出貨|已完成|已取消|退貨中|已退款)/);
  42 |         return m?.[1] ?? '';
  43 |       })
  44 |       .toBe('已取消');
  45 | 
  46 |     await expect(page.getByRole('button', { name: '取消訂單' })).toHaveCount(0);
  47 |     await expect(page.getByRole('button', { name: '模擬出貨（Demo）' })).toHaveCount(0);
  48 |     await expect(page.getByText(orderId)).toBeVisible();
  49 |   });
  50 | });
  51 | 
```