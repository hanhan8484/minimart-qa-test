# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: ui\c-checkout-validation.spec.ts >> C-B07 checkout shipping validation >> DEF-011: address validation should show red error texts
- Location: tests\ui\c-checkout-validation.spec.ts:67:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText('請輸入收件地址')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByText('請輸入收件地址')

```

```yaml
- banner:
  - link "MiniMart":
    - /url: /
  - navigation:
    - link "商品":
      - /url: /
    - link "購物車 1":
      - /url: /cart
    - link "我的訂單":
      - /url: /orders
    - link "我的優惠卷":
      - /url: /coupons
    - link "通知中心 3":
      - /url: /notifications
    - text: demo@minimart.test
    - button "登出"
- main:
  - heading "結帳" [level=1]
  - heading "收件資訊" [level=2]
  - text: 收件人姓名
  - textbox "收件人姓名": 測試
  - text: 手機號碼
  - textbox "手機號碼": "0912345678"
  - text: 收件地址
  - textbox "收件地址"
  - heading "訂單備註" [level=2]
  - text: 訂單備註（選填）
  - textbox "訂單備註（選填）"
  - paragraph: 0/100
  - heading "優惠券" [level=2]
  - radio "不使用優惠券" [checked]
  - text: 不使用優惠券
  - radio "新人小禮券 （NT$20，門檻 NT$0，效期 2027-01-17）"
  - text: 新人小禮券 （NT$20，門檻 NT$0，效期 2027-01-17） 滿千折百券 （NT$100，門檻 NT$1,000，效期 2027-01-17） 未達使用門檻 NT$1,000 滿三千折三百券 （NT$300，門檻 NT$3,000，效期 2027-01-17） 未達使用門檻 NT$3,000 全站 85 折券 （15%，門檻 NT$800，效期 2027-01-17） 未達使用門檻 NT$800
  - radio "免運券 （免運，門檻 NT$0，效期 2027-01-17）"
  - text: 免運券 （免運，門檻 NT$0，效期 2027-01-17） 舊版折五十券 （NT$50，門檻 NT$0，效期 2026-06-21） 已過期
  - heading "付款方式" [level=2]
  - paragraph: 貨到付款
  - text: 商品小計 NT$480 滿額折扣 NT$0 優惠券折抵 NT$0 運費 NT$80 應付金額 NT$560
  - button "送出訂單" [disabled]
```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test';
  2   | import {
  3   |   applyPricingCaseCart,
  4   |   checkoutNoteField,
  5   |   loginAsDemo,
  6   |   resetEnv,
  7   | } from '../helpers';
  8   | import { CASE_SHIP_LT500 } from '../fixtures/pricing-cases';
  9   | 
  10  | /**
  11  |  * Batch 10 — C-B07 checkout shipping field validation
  12  |  * R-12.2, R-12.6, R-18.1～R-18.5, R-18.7～R-18.9
  13  |  */
  14  | test.describe('C-B07 checkout shipping validation', () => {
  15  |   test.beforeAll(async ({ request }) => {
  16  |     test.setTimeout(60_000);
  17  |     await resetEnv(request);
  18  |   });
  19  | 
  20  |   test.beforeEach(async ({ page }) => {
  21  |     await loginAsDemo(page);
  22  |     await applyPricingCaseCart(page, CASE_SHIP_LT500);
  23  |     await page.goto('/checkout');
  24  |     await expect(page.locator('.checkout-page')).toBeVisible();
  25  |   });
  26  | 
  27  |   test('empty / bad fields disable submit; name & phone show exact errors', async ({ page }) => {
  28  |     const submit = page.locator('.checkout-submit-btn');
  29  |     await expect(submit).toBeDisabled();
  30  | 
  31  |     await page.locator('#checkout-name').fill('');
  32  |     await page.locator('#checkout-phone').fill('123');
  33  |     await page.locator('#checkout-address').fill('短');
  34  |     await page.locator('#checkout-name').blur();
  35  |     await page.locator('#checkout-phone').blur();
  36  |     await page.locator('#checkout-address').blur();
  37  | 
  38  |     await expect(submit).toBeDisabled();
  39  |     await expect(page.getByText('請輸入收件人姓名')).toBeVisible();
  40  |     await expect(
  41  |       page.getByText('請輸入正確的手機號碼（09 開頭，共 10 位數字）'),
  42  |     ).toBeVisible();
  43  |   });
  44  | 
  45  |   test('name over 20 chars not truncated; shows exact error', async ({ page }) => {
  46  |     const longName = '甲'.repeat(25);
  47  |     await page.locator('#checkout-name').fill(longName);
  48  |     expect(await page.locator('#checkout-name').inputValue()).toHaveLength(25);
  49  |     await page.locator('#checkout-name').blur();
  50  |     await expect(page.getByText('收件人姓名不可超過 20 個字')).toBeVisible();
  51  |     await expect(page.locator('.checkout-submit-btn')).toBeDisabled();
  52  |   });
  53  | 
  54  |   test('trim allows submit when padded name is otherwise valid', async ({ page }) => {
  55  |     await page.locator('#checkout-name').fill('  測試收件  ');
  56  |     await page.locator('#checkout-phone').fill('0912345678');
  57  |     await page.locator('#checkout-address').fill('台北市信義區測試路一段100號');
  58  |     await expect(page.locator('.checkout-submit-btn')).toBeEnabled();
  59  |   });
  60  | 
  61  |   test('address over 100 chars not truncated', async ({ page }) => {
  62  |     const longAddr = '北'.repeat(101);
  63  |     await page.locator('#checkout-address').fill(longAddr);
  64  |     expect(await page.locator('#checkout-address').inputValue()).toHaveLength(101);
  65  |   });
  66  | 
  67  |   test('DEF-011: address validation should show red error texts', async ({ page }) => {
  68  |     test.fail(
  69  |       true,
  70  |       'DEF-011: 收件地址空／過短／過長時不顯示 R-18.8 紅字（僅停用送出）（R-18.7）',
  71  |     );
  72  | 
  73  |     await page.locator('#checkout-name').fill('測試');
  74  |     await page.locator('#checkout-phone').fill('0912345678');
  75  | 
  76  |     await page.locator('#checkout-address').fill('');
  77  |     await page.locator('#checkout-address').blur();
> 78  |     await expect(page.getByText('請輸入收件地址')).toBeVisible();
      |                                             ^ Error: expect(locator).toBeVisible() failed
  79  | 
  80  |     await page.locator('#checkout-address').fill('短址');
  81  |     await page.locator('#checkout-address').blur();
  82  |     await expect(page.getByText('收件地址須為 5 至 100 個字')).toBeVisible();
  83  | 
  84  |     await page.locator('#checkout-address').fill('北'.repeat(101));
  85  |     await page.locator('#checkout-address').blur();
  86  |     await expect(page.getByText('收件地址須為 5 至 100 個字')).toBeVisible();
  87  |   });
  88  | 
  89  |   test('DEF-022: phone longer than 10 digits must be rejected', async ({ page }) => {
  90  |     test.fail(
  91  |       true,
  92  |       'DEF-022: 手機 11 位（如 09123456789）仍可啟用送出、無錯誤（R-18.4）',
  93  |     );
  94  |     await page.locator('#checkout-name').fill('測試');
  95  |     await page.locator('#checkout-phone').fill('09123456789');
  96  |     await page.locator('#checkout-address').fill('台北市信義區測試路一段100號');
  97  |     await page.locator('#checkout-phone').blur();
  98  |     await expect(
  99  |       page.getByText('請輸入正確的手機號碼（09 開頭，共 10 位數字）'),
  100 |     ).toBeVisible();
  101 |     await expect(page.locator('.checkout-submit-btn')).toBeDisabled();
  102 |   });
  103 | });
  104 | 
  105 | /**
  106 |  * v2.1 新增 — C-B13 訂單備註驗證（R-18.10）
  107 |  * 與 C-B07 同分檔（結帳驗證主題），案例 ID 獨立避免與 C-B07／C-B11 重號。
  108 |  */
  109 | test.describe('C-B13 checkout order-note validation (v2.1)', () => {
  110 |   test.beforeAll(async ({ request }) => {
  111 |     test.setTimeout(60_000);
  112 |     await resetEnv(request);
  113 |   });
  114 | 
  115 |   test.beforeEach(async ({ page }) => {
  116 |     await loginAsDemo(page);
  117 |     await applyPricingCaseCart(page, CASE_SHIP_LT500);
  118 |     await page.goto('/checkout');
  119 |     await expect(page.locator('.checkout-page')).toBeVisible();
  120 |     await expect(page.locator('#checkout-name')).toBeVisible({ timeout: 20_000 });
  121 |     await page.locator('#checkout-name').fill('測試收件人');
  122 |     await page.locator('#checkout-phone').fill('0912345678');
  123 |     await page.locator('#checkout-address').fill('台北市信義區測試路一段100號');
  124 |     await expect(checkoutNoteField(page)).toBeVisible({ timeout: 10_000 });
  125 |   });
  126 | 
  127 |   test('C-B13: over 100 chars shows red error and disables submit', async ({ page }) => {
  128 |     const note = checkoutNoteField(page);
  129 |     const over = '測'.repeat(101);
  130 |     await note.fill(over);
  131 |     await expect(page.getByText('101/100')).toBeVisible();
  132 |     await expect(page.getByText('訂單備註不可超過 100 個字')).toBeVisible();
  133 |     await expect(page.locator('.checkout-submit-btn')).toBeDisabled();
  134 |   });
  135 | });
  136 | 
```